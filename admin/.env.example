# Copy to .env and fill in real values. Vite only exposes variables prefixed with VITE_.

# Backend API base URL (see ../backend). Required — this app has no offline/mock mode,
# unlike the storefront, since it only exists to talk to the real API.
VITE_API_URL=http://localhost:4000
